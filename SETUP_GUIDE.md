# フォーム営業リサーチ自動化ツール（GitHub Actions版）セットアップ手順

毎朝決まった時刻に、自動でフォーム営業リサーチを実行するための設定手順です。
一度設定すれば、あとはあなたが何もしなくても、毎朝リストが自動で更新されます。

パソコン初心者の方向けに、ひとつずつ詳しく書いています。

---

## 全体の流れ（4つの山）

- 山1：GitHubアカウントを作る　← 完了済み
- 山2：ツールを置く「箱」（リポジトリ）を作って、ファイルを入れる
- 山3：秘密の鍵（APIキーなど）を安全に登録する
- 山4：動作確認をして、毎朝自動で動く状態にする

この手順書は山2から始まります。

---

## 事前に手元に用意するもの

次の5つがそろっているか確認してください。

1. GitHubアカウント（山1で作成済み）
2. SerpAPIキー（取得済みの長い文字列）
3. スプレッドシートID（保存先シートのURLの真ん中部分）
4. サービスアカウントのJSONファイル（パソコンに保存されている鍵ファイル）
5. 専用Googleアカウント（スプレッドシートを作ったアカウント）

---

# 山2　ツールを置く「箱」を作る

GitHubでは、ツール一式を入れる入れ物を「リポジトリ」と呼びます。まずこれを作ります。

## 2-1. リポジトリを新規作成する

1. ブラウザで https://github.com を開き、ログインした状態にします
2. 画面右上の「＋」マークをクリックし、「New repository」を選びます
3. 「Repository name」に名前を入れます（例：`form-sales-researcher`）。半角英数字で。
4. 「Public」か「Private」を選びます。**Public（公開）** を選んでください。
   - 公開にするとGitHub Actionsが無料で使えます。
   - コード本体は他人から見えますが、秘密の鍵は山3で別の安全な場所に入れるので問題ありません。
5. 他の項目はそのままで、「Create repository」ボタンを押します

## 2-2. ツールのファイルを入れる

リポジトリができたら、ツールのファイル一式をアップロードします。

1. できたリポジトリの画面に「uploading an existing file」というリンクがあります。これをクリックします。
   - 見つからない場合は、「Add file」→「Upload files」を選んでも同じです。
2. お渡しした `form-sales-researcher` フォルダの**中身**を、ファイルをアップロードする画面にドラッグ＆ドロップします。
   - フォルダごとではなく、フォルダを開いた中身（`src` フォルダや `config.yaml` など）を入れてください。
3. アップロードが終わったら、画面下の「Commit changes」ボタンを押します
4. リポジトリの画面に `src` や `config.yaml`、`.github` などが並んでいれば成功です

---

# 山3　秘密の鍵を安全に登録する

SerpAPIキーやサービスアカウントの鍵は、コードと一緒に公開してはいけません。
GitHubには「Secrets（シークレット）」という、鍵専用の安全な隠し場所があります。
ここに3つの秘密情報を登録します。

## 3-1. Secrets の画面を開く

1. リポジトリの画面上部の「Settings」（設定）タブをクリックします
2. 左側のメニューから「Secrets and variables」→「Actions」を選びます
3. 「New repository secret」というボタンが見えます

## 3-2. 3つの秘密を登録する

「New repository secret」を押して、次の3つを1つずつ登録します。
それぞれ「Name」（名前）と「Secret」（中身）を入力して「Add secret」を押します。

### 秘密その1：SerpAPIキー
- Name に： `SERPAPI_API_KEY`
- Secret に： あなたのSerpAPIキーの文字列

### 秘密その2：スプレッドシートID
- Name に： `GOOGLE_SHEET_ID`
- Secret に： あなたのスプレッドシートID

### 秘密その3：サービスアカウントのJSON
- Name に： `GOOGLE_SERVICE_ACCOUNT_JSON`
- Secret に： サービスアカウントのJSONファイルの**中身まるごと**
  - JSONファイルをメモ帳などで開き、`{` から `}` まで全部をコピーして貼り付けます

3つとも登録できると、Secrets の一覧に3行表示されます。
（登録後、中身は二度と表示されません。これは正常です。安全のための仕様です。）

---

# 山4　動作確認と自動化

## 4-1. まず手動で1回動かしてみる

いきなり自動に任せず、まず手動で動かして成功を確認します。

1. リポジトリ上部の「Actions」タブをクリックします
2. 左側に「フォーム営業リサーチ」というワークフローが表示されます。クリックします。
3. 右側に「Run workflow」というボタンがあります。これを押し、もう一度「Run workflow」を押します
4. しばらくすると一覧に実行中の行が現れます（黄色い丸印）
5. 数分待ち、緑のチェックマークになれば成功です。赤い×なら失敗です。

## 4-2. 結果を確認する

- スプレッドシートを開き、`Prospects` タブにデータが入っているか確認します
- GitHubの実行結果の画面からも、`prospects-csv` という成果物をダウンロードできます

## 4-3. 毎朝の自動実行について

`.github/workflows/research.yml` の中で、実行時刻が設定されています。
初期設定は「毎朝9時（日本時間）」です。手動実行が成功していれば、
明日の朝から自動で動き始めます。あなたは何もしなくて構いません。

時刻を変えたい場合は、`research.yml` の `cron` の行を編集します。
GitHub Actions の時刻は世界標準時（UTC）で、日本時間より9時間遅れます。
例：日本時間の朝7時に動かしたいなら、UTCの前日22時 → `cron: "0 22 * * *"`

---

# 設定の調整

調べる業種・都道府県・件数は `config.yaml` で変えられます。
GitHubのリポジトリ画面で `config.yaml` を開き、鉛筆マークで編集できます。

特に大事な設定：
- `prefectures`：調べる都道府県。1県ずつ進めるなら、ここを1つにします
- `industries`：調べる業種
- `limit_per_query`：1回の件数。最初は小さめ（5〜10）を推奨
- `filter_by_prefecture`：true なら指定した県の会社だけ残します

「長崎県が終わったら次は熊本県」と進めたいときは、`config.yaml` の
`prefectures` を書き換えるだけです。

---

# うまくいかないときは

- Actions の実行が赤い×になったら、その行をクリックすると英語のログが見られます。
  どこで失敗したか、画面の様子を伝えてもらえれば一緒に解決できます。
- スプレッドシートにデータが入らないときは、山3の3つの秘密、特に
  サービスアカウントのJSONが正しく登録されているか、そしてスプレッドシートが
  サービスアカウントのメールアドレスに「編集者」で共有されているか確認してください。

困ったら、画面のスクリーンショットを送ってください。一緒に進めます。
